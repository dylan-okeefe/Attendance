<!DOCTYPE html>
<html>
<head>
<h2 class="navbar-fixed-top">//Attendr</h2>
  <nav class="navbar navbar-inverse">
    <div class="container-fluid" id="button">
          <div class="navbar-header">
            <a class="navbar-brand" href="#">Student</a>
            <div id = 'list' class="hide">
              <ul id = 'list-ul'>
                <li>Records</li>
                <li>Courses</li>
                <li>Learn</li>
              </ul>
            </div>
          </div>
        <div>
          <ul class="nav navbar-nav">
            <li class="active"><a href="#">Home</a></li>
            <%if !user_signed_in? %>  
              <li><%= link_to("Log In", new_user_session_path) %></li>
              <li><%= link_to("Sign Up", new_user_registration_path) %></li>
            <%else%>
              <li><%= link_to("Log Out", destroy_user_session_path, method: :delete) %></li>
            <%end%>
          </ul>
        </div> 
    </div>
  </nav>

</head>
<body>


  <div id="centered">
    <button type="button" class="btn btn-primary btn-lg outline" id="button" onclick="attend();">
      <h1>Check In</h1>
    </button> 
  </div>
  
  <script>
    // $('#centered button').click(function(){
    //   alert('omrika');
    // });

  function attend(){
    var form = document.createElement('form');
    // form.setAttribute('method', 'post');
    form.setAttribute('action', 'attendences');
    form.style.display = 'hidden';
    document.body.appendChild(form)
    form.submit();
  }
  </script>
  
</body>
</html>