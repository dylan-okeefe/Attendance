<% if user_signed_in? %>
  <h1><%= gravatar_for current_user %></h1>
<%end%>

<%if user_signed_in?%>
  <div id="centered">
    <%= button_to "Get To Class", {controller: "attendances", action: "create"}, form_class: "btn btn-primary btn-lg outline", disabled: !@clickable %>   
  </div>
<%end%>