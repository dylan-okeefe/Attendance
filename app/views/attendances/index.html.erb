<!DOCTYPE html>
<html>
<head>
<h2 class="navbar-fixed-top"></h2>
  <nav class="navbar navbar-inverse">
    <div class="container-fluid" id="button">
          <div class="navbar-header">
            <a class="navbar-brand" href="#">Student</a>
            <div id = 'list' class="hide">
              <ul id = 'list-ul'>
                <li>Records</li>
                <li>Courses</li>
                <li>Learn</li>
              </ul>
            </div>
          </div>
        <div>
          <ul class="nav navbar-nav">
            <li class="active"><a href="#">Home</a></li>
            <% if !user_signed_in? %>  
              <li><%= link_to("Log In", new_user_session_path) %></li>
              <li><%= link_to("Sign Up", new_user_registration_path) %></li>
            <%else%>
              <li><%= link_to("Log Out", destroy_user_session_path, method: :delete) %></li>
            <%end%>
          </ul>
        </div> 
    </div>
  </nav>

</head>
<body>
 <%= render 'form' %>

<div id="pieChart"></div>
<!-- <form>
  date <input type='text' name='date'><%= Time.now.strftime('%m/%d/%y') %></input>
  time <input type='text' name='time'><%= Time.now.strftime('%I:%M %p') %></input>
</form>  -->
<!-- if the time now is after 9:15 am your late if its before you're present.
rails form. If student press submit with above comparison logic then do this thing.
 -->


</body>
</html>
